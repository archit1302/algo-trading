# Topic 1: Introduction to Python & APIs – Assignments

1. **Verify Your Setup**
   - Run `python --version`.
   - Create and run `hello.py` with your own greeting.

2. **Variables & Math**
   - Create `calc.py` that:
     ```python
     # ask user for two numbers, then
     # print sum, difference, product, quotient
     ```
   - Test with at least two different pairs.

3. **Working with Strings**
   - Write `join_names.py` that:
     ```python
     # asks for first and last name
     # prints "Hello, <first> <last>!"
     ```

4. **Simple Function**
   - In `utils.py`, define:
     ```python
     def square(x):
         # return x squared
     ```
   - Write a small script that uses `square` on user input and prints result.

5. **API Theory Quiz**
   - In a Markdown file `api_quiz.md`, answer:
     1. Define “API” in your own words.
     2. Explain the restaurant-waiter analogy.
     3. List one example of an API you’ve used (e.g. opening a website, logging into an app).
